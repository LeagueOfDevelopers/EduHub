// import { fromJS } from 'immutable';
// import { selectRegistrationAcceptedDomain } from '../selectors';

describe('selectRegistrationAcceptedDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
